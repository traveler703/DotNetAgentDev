# 项目选题：多 Agent 旅游规划系统

项目成员：2351458 任泓臻 2352979 虞澍 2353117 黄彦炜 2354283黄迪

## 一、项目背景

随着旅游需求增长，用户在规划旅行时通常需要同时考虑目的地、天数、预算、住宿、交通、天气、签证、安全风险等多个因素。传统旅游攻略信息分散，用户需要在多个平台之间反复查询和比较，规划成本较高。

本项目拟基于 `.NET 10` 开发一个多 Agent 旅游规划系统。用户只需输入旅行目的地、旅行天数、预算、出发地、偏好和备注信息，系统即可通过多个专业 Agent 协作，自动生成一份结构化、可解释、符合预算约束的详细旅行计划。

## 二、项目目标

本系统的核心目标是实现一个能够自主推理、规划并调用工具完成任务的 AI Agent 系统。

用户输入示例：

> 我想从上海出发去日本玩 7 天，预算 12000 元，喜欢美食和轻松一点的路线，不想太赶。

系统输出内容包括：

- 每日行程安排
- 景点路线设计
- 酒店住宿建议
- 城市内交通方案
- 总预算估算
- 天气、签证、安全等风险提醒
- 可调整建议，例如降低预算、增加舒适度或减少路程

## 三、系统总体架构

系统采用多 Agent 协作架构，由一个主控 Agent 负责任务分解和结果整合，多个专业 Agent 负责不同子任务。

核心组件包括：

- **LLM 模块**：调用 DeepSeek API 完成自然语言理解、推理和生成。
- **Agent Loop 模块**：实现 Thought → Action → Observation 的 ReAct 循环。
- **工具调用模块**：提供景点查询、酒店查询、交通估算、预算计算、风险检查等工具。
- **Memory 模块**：保存用户输入、偏好、历史规划结果和中间状态。
- **Web 用户界面**：提供旅行需求输入、规划过程展示和最终行程展示。

## 四、Agent 设计

### 4.1 主控 Agent：Travel Coordinator Agent

职责：

- 理解用户输入的旅行需求。
- 将任务拆分给各个专业 Agent。
- 协调不同 Agent 的输出结果。
- 处理冲突，例如预算超支、路线过满、交通时间过长。
- 生成最终统一行程方案。

### 4.2 行程规划 Agent

职责：

- 根据目的地、天数和用户偏好设计每日路线。
- 安排景点、美食、休息时间。
- 控制每天行程强度。
- 避免路线重复和不合理绕路。

可调用工具：

- 景点查询工具
- 景点分类工具
- 路线排序工具

### 4.3 酒店 Agent

职责：

- 根据预算、目的地和行程区域推荐住宿。
- 判断住宿位置是否方便。
- 给出不同价位的酒店建议。

可调用工具：

- 酒店模拟查询工具
- 酒店价格估算工具
- 住宿区域推荐工具

### 4.4 交通 Agent

职责：

- 规划出发地到目的地的大交通方案。
- 估算市内交通时间和费用。
- 检查每日路线交通是否合理。

可调用工具：

- 城市间交通估算工具
- 市内交通估算工具
- 路线耗时估算工具

### 4.5 预算 Agent

职责：

- 汇总酒店、交通、餐饮、门票等费用。
- 判断是否超出预算。
- 在超预算时提出调整建议。
- 生成预算明细表。

可调用工具：

- 预算计算工具
- 预算检查工具
- 费用优化工具

### 4.6 风险 Agent

职责：

- 检查目的地天气、签证、安全、季节性风险。
- 提醒用户携带物品或注意事项。
- 对不合理计划提出风险提示。

可调用工具：

- 天气查询工具
- 签证规则查询工具
- 旅行风险检查工具

## 五、工具设计

项目至少实现以下自定义工具，满足项目要求中“至少 3 个工具”的规定。

| 工具名称     | 功能说明                                     |
| ------------ | -------------------------------------------- |
| 景点查询工具 | 根据目的地和偏好返回景点、美食、商圈等候选点 |
| 酒店查询工具 | 根据城市、预算和区域返回住宿建议             |
| 交通估算工具 | 估算城市间交通、市内交通时间和费用           |
| 预算计算工具 | 汇总住宿、交通、餐饮、门票等费用             |
| 天气查询工具 | 查询或模拟目的地未来天气情况                 |
| 风险检查工具 | 检查签证、天气、安全、行程强度等风险         |
| 行程保存工具 | 保存用户需求和最终规划结果，实现记忆机制     |

如果担心实时 API 不稳定，可以先使用本地 JSON 数据模拟景点、酒店、交通和风险信息；后续再扩展真实接口。

## 六、Agent Loop 设计

系统采用 ReAct 模式，每个 Agent 在执行任务时遵循以下流程：

1. **Thought**：分析当前任务，例如“用户预算较低，需要优先选择经济型酒店”。
2. **Action**：选择并调用合适工具，例如调用酒店查询工具。
3. **Observation**：读取工具返回结果，例如酒店价格和位置。
4. **Next Thought**：根据结果继续判断是否满足预算和路线要求。
5. **Final Answer**：输出该 Agent 的子任务结果。

主控 Agent 会收集所有专业 Agent 的结果，并进行最终整合。

## 七、Memory 设计

本项目设计两类记忆：

- **短期记忆**：保存当前会话中的用户输入、各 Agent 中间结果和工具调用结果。
- **长期记忆**：保存用户历史偏好，例如喜欢美食、预算偏低、不喜欢赶路、偏好市中心酒店等。

Memory 可先使用 JSON 文件或 SQLite 实现，便于开发和答辩解释。

## 八、技术选型

- 开发语言：C#
- 框架：`.NET 10`
- LLM：DeepSeek API
- Agent 框架：Semantic Kernel 或自定义 Agent Loop
- 前端界面：ASP.NET Core Razor Pages / Blazor / Web API + 简单前端
- 数据存储：SQLite 或本地 JSON 文件
- 日志：Microsoft.Extensions.Logging
- 配置：appsettings.json + 环境变量
- 异步编程：所有 LLM 调用和工具调用使用 `async/await`

## 九、项目亮点

- **多 Agent 协作**：不同 Agent 分工明确，符合加分项要求。
- **工具调用丰富**：旅游规划天然需要调用多种工具。
- **结果可解释**：可以展示每个 Agent 的推理过程和工具调用结果。
- **实用性强**：用户输入简单，输出结果直观。
- **易于演示**：现场可以输入不同预算和天数，展示计划如何变化。

## 十、四人分工

| 成员   | 主要职责                                                |
| ------ | ------------------------------------------------------- |
| 成员 A | 负责 DeepSeek API 接入、主控 Agent、Agent Loop          |
| 成员 B | 负责行程规划 Agent、酒店 Agent、相关工具                |
| 成员 C | 负责交通 Agent、预算 Agent、风险 Agent                  |
| 成员 D | 负责 Web 界面、Memory、日志、README、架构文档和反思报告 |

## 十一、开发计划

### 第 1 阶段：需求分析与架构设计

- 明确输入字段和输出格式。
- 设计多 Agent 架构。
- 确定工具列表和数据结构。
- 完成架构图和流程图初稿。

### 第 2 阶段：核心 Agent Loop 实现

- 接入 DeepSeek API。
- 实现 ReAct 循环。
- 实现工具注册与调用。
- 完成基础命令行或 Web 测试入口。

### 第 3 阶段：专业 Agent 与工具开发

- 实现行程规划 Agent。
- 实现酒店、交通、预算、风险 Agent。
- 准备本地景点、酒店、交通、天气模拟数据。
- 实现预算检查和风险提醒。

### 第 4 阶段：Web 界面与记忆机制

- 实现用户输入页面。
- 展示 Agent 推理过程和最终行程。
- 保存用户历史偏好和历史规划。
- 增加日志和错误处理。

### 第 5 阶段：测试、文档与答辩准备

- 测试不同目的地、预算、天数输入。
- 编写 README。
- 编写架构设计文档。
- 编写反思报告，重点解释 Agent Loop。
- 准备 10 分钟演示和 5 分钟答辩材料。

## 十二、预期演示流程

1. 用户打开 Web 页面。
2. 输入旅行需求：目的地、天数、预算、出发地、偏好。
3. 系统显示任务拆解过程。
4. 各 Agent 分别生成路线、住宿、交通、预算、风险结果。
5. 主控 Agent 整合为完整旅行计划。
6. 如果预算超出，预算 Agent 给出调整方案。
7. 用户查看最终行程和预算明细。

## 十三、风险与控制

| 风险                | 解决方案                                  |
| ------------------- | ----------------------------------------- |
| 第三方 API 不稳定   | 优先使用本地模拟数据，真实 API 作为扩展   |
| 多 Agent 逻辑过复杂 | 主控 Agent 先串行调用各 Agent，后续再优化 |
| LLM 输出格式不稳定  | 使用固定 JSON 输出格式和结果校验          |
| 预算估算不准确      | 明确说明为参考估算，并展示计算来源        |
| 答辩难以解释        | 保持核心 Agent Loop 简洁，写逐行解读文档  |

## 十四、加分项设计

本项目可自然覆盖多个加分项：

- **多 Agent 协作**：主控 Agent + 行程 / 酒店 / 交通 / 预算 / 风险 Agent。
- **RAG**：加入本地旅游知识库，例如城市攻略、景点介绍、签证政策文本。
- **单元测试**：测试预算计算、工具调用、行程格式校验。
- **可观测性**：在页面展示每一步 Thought、Action、Observation。
- **创新性应用**：结合预算、风险和用户偏好生成个性化旅行计划。

## 十五、结论

“多 Agent 旅游规划系统”非常适合作为本课程期末项目。它既符合“基于 .NET 的 AI Agent 开发”的核心要求，又能充分展示多 Agent 协作、工具调用、记忆机制和推理流程。项目规模适合 4 人小组完成，演示效果明显，答辩时也容易围绕 Agent Loop、工具设计和系统架构展开说明。
